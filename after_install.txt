- # apt install sudo
- # adduser ibrahim sudo
- # nano /etc/apt/sources.list
- # apt update && apt upgrade
- # apt fullupgrade
- make swap file:
	$ sudo fallocate -l 2G /swapfile
	$ sudo chmod 600 /swapfile
	$ sudo mkswap /swapfile
	$ sudo swapon /swapfile
- edit fstab
	# mkdir /mnt/all_files
	# nano /etc/fstab
- # check swappiness property:
	$ cat /proc/sys/vm/swappiness
- # change swappiness:
	$ sudo nano /etc/sysctl.conf
		#SET SWAPPINESS
		vm.swappiness=0
- $ sudo apt install intel-microcode firmware-amd-graphics firmware-iwlwifi firmware-misc-nonfree
- $ sudo apt install i3 xinput xbacklight git scrot htop build-essential gnome-mpv vlc fonts-beng clang ipython3 ttf-mscorefonts-installer ttf-dejavu powertop, tlp, smartmontools profile-sync-daemon
- edit profile-sync-daemon
	$ psd
	$ vi /home/ibrahim/.config/psd/psd.conf
		USE_OVERLAYFS="yes"
		USE_BACKUPS="yes"
		BACKUP_LIMIT=5
	$ visudo /etc/sudoers
		ibrahim ALL=(ALL) NOPASSWD: /usr/bin/psd-overlay-helper
	$ systemctl --user enable psd.service
	$ systemctl --user start psd.service
	$ psd p
- $ create file to control brightness in i3:
	$ sudo nano /etc/X11/xorg.conf
		Section "Device"
		Identifier "Card0"
		Driver "intel"
		Option "backlight" "/sys/devices/pci0000:00/0000:00:02.0/drm/card0/card0-eDP-1/intel_backlig$
		EndSection
- $ sudo apt install ./vscode ./chrome
- copy .config folders for Code, i3 and i3Status
- copy .vscode folder onto home
- cp bashrc ~/.bashrc
- cp bash_aliases ~/.bash_aliases
- cp .gitconfig ~/
- Install lastpass
- install anki
- $ sudo cp ~/Downloads/skl_dmc_ver1_27.bin /lib/firmware/i915/
- Edit grub menu:
	$ sudo nano /etc/default/grub
	$ sudo update-grub
- $ ln -s /mnt/all_files/soft ~/soft
- $ ln -s /mnt/all_files/work ~/work
- $ ln -s /mnt/all_files/others ~/others
- $ ln -s /mnt/all_files/soft /soft
- $ ln -s /mnt/all_files/work /work
- $ ln -s /mnt/all_files/others /others
- Change permissions:
	find . -type d -exec chmod 755 {} \;
	find . -type f -exec chmod 644 {} \;